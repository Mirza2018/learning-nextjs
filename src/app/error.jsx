'use client'
import React from 'react';

const error = () => {
    console.log(name);
    
    return (
        <div>
            error
            error
            error
            error
            error
            error
            error
            error
        </div>
    );
};

export default error;