import React from 'react';

const VipPage    = ({params}) => {
    console.log(params);
    
    return (
        <div>
            This is mutiple dynamic pages
        </div>
    );
};

export default VipPage  ;